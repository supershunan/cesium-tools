# 为什么数据映射是 data[time][level][y][x] 而不是 data[time][level][x][y]

## 1. 数据读取顺序分析

查看 `GridDataReader.readGridData()` 方法（第197-224行）：

```typescript
for (let t = 0; t < times; t++) {
    // 外层循环：时间
    data[t] = new Array(levels);

    for (let l = 0; l < levels; l++) {
        // 第二层：层次
        data[t][l] = new Array(ySize);

        for (let y = 0; y < ySize; y++) {
            // 第三层：y方向（纬度，行）
            data[t][l][y] = new Array(xSize);

            for (let x = 0; x < xSize; x++) {
                // 最内层：x方向（经度，列）
                data[t][l][y][x] = finalValue;
            }
        }
    }
}
```

**循环嵌套顺序**：`times → levels → y → x`

所以数据结构是：`data[time][level][y][x]`

## 2. 为什么是这样的顺序？

### 2.1 文件数据存储方式（行优先 Row-Major）

原始数据文件通常是按照**行优先（Row-Major）**顺序存储的：

```
文件中的数据顺序：
┌─────────────────────────────────────────┐
│ time0-level0:                          │
│   row0: [x0, x1, x2, ..., xN]         │  ← 第一行（y=0）的所有x值
│   row1: [x0, x1, x2, ..., xN]         │  ← 第二行（y=1）的所有x值
│   row2: [x0, x1, x2, ..., xN]         │  ← 第三行（y=2）的所有x值
│   ...                                  │
│   rowM: [x0, x1, x2, ..., xN]         │  ← 最后一行（y=M）
│ time0-level1: ...                      │
│ ...                                    │
└─────────────────────────────────────────┘
```

这种方式的好处：

-   ✅ **缓存友好**：同一行的数据在内存中连续存储，访问效率高
-   ✅ **符合常见格式**：NetCDF、HDF5、GRIB等科学数据格式都使用行优先
-   ✅ **地理意义**：按纬度行扫描，符合扫描顺序（从上到下）

### 2.2 列优先（Column-Major）对比

如果使用 `data[time][level][x][y]`（列优先）：

```
文件中的数据顺序：
┌─────────────────────────────────────────┐
│ time0-level0:                          │
│   col0: [y0, y1, y2, ..., yM]         │  ← 第一列（x=0）的所有y值
│   col1: [y0, y1, y2, ..., yM]         │  ← 第二列（x=1）的所有y值
│   ...                                  │
│   colN: [y0, y1, y2, ..., yM]         │  ← 最后一列（x=N）
└─────────────────────────────────────────┘
```

这种方式的问题：

-   ❌ 访问一行数据时需要跳跃读取，缓存命中率低
-   ❌ 不符合大多数科学数据格式的存储方式
-   ❌ 处理图像/网格数据时不常用

## 3. 数据访问示例

### 3.1 当前方式（行优先）：`data[t][l][y][x]`

访问一个完整的纬度行（所有经度值）：

```typescript
const row = data[0][0][yIndex]; // 一次性获取整行，内存连续
// row = [value_x0, value_x1, value_x2, ..., value_xN]
```

### 3.2 如果改为列优先：`data[t][l][x][y]`

访问一个完整的纬度行（所有经度值）：

```typescript
const row = [];
for (let x = 0; x < xSize; x++) {
    row.push(data[0][0][x][yIndex]); // 需要跳跃访问，缓存不友好
}
```

## 4. 内存布局对比

### 4.1 行优先 `[y][x]` (当前方式)

```
内存地址（连续）：
┌─────┬─────┬─────┬─────┬─────┬─────┐
│ y0x0│ y0x1│ y0x2│ y1x0│ y1x1│ y1x2│  ← 按行连续存储
└─────┴─────┴─────┴─────┴─────┴─────┘
  ↑同一行数据内存连续，缓存命中率高
```

### 4.2 列优先 `[x][y]` (假设方式)

```
内存地址（跳跃）：
┌─────┬─────┬─────┬─────┬─────┬─────┐
│ x0y0│ x1y0│ x2y0│ x0y1│ x1y1│ x2y1│  ← 按列连续存储
└─────┴─────┴─────┴─────┴─────┴─────┘
  ↑访问一行需要跳跃，缓存命中率低
```

## 5. 实际使用场景

### 5.1 渲染时的访问模式

在体素渲染中，我们通常需要：

-   访问整个网格的某个区域（矩形块）
-   按行或按块访问数据
-   进行空间插值（需要相邻像素）

行优先存储 `[y][x]` 更适合这些场景。

### 5.2 分块处理

当前的分块代码：

```typescript
// 按行分块（y方向）
const yChunkData = tempData.slice(yStartSlice, yEndSlice);

// 对每行的列进行切片（x方向）
const chunkData = yChunkData.map((row: number[]) => {
    return row.slice(xStartSlice, xEndSlice);
});
```

这种方式利用了行优先存储的优势，可以高效地切取矩形区域。

## 6. 结论

**为什么是 `data[time][level][y][x]` 而不是 `data[time][level][x][y]`？**

1. ✅ **符合数据文件格式**：原始数据文件使用行优先存储
2. ✅ **性能优势**：同一行的数据在内存中连续，访问效率高
3. ✅ **符合惯例**：科学数据格式（NetCDF、HDF5等）普遍使用行优先
4. ✅ **便于处理**：访问矩形区域、分块等操作更高效
5. ✅ **地理意义**：按纬度行扫描，符合数据采集和处理顺序

这是一个**设计决策**，基于性能、兼容性和实用性的综合考虑。

## 7. 相关概念

-   **Row-Major Order（行优先）**：C/C++、Python、JavaScript等语言的多维数组默认方式
-   **Column-Major Order（列优先）**：Fortran、MATLAB、R等语言使用
-   **内存对齐**：行优先存储使行数据对齐，提高缓存性能
